# Управление проектом Docker

## 🚀 Запуск проекта

### Windows:
```bash
start.bat
```

### Linux/Mac:
```bash
chmod +x start.sh
./start.sh
```

### Через команду:
```bash
docker-compose up -d
```

---

## ⏹️ Остановка проекта

### Windows:
```bash
stop.bat
```

### Linux/Mac:
```bash
chmod +x stop.sh
./stop.sh
```

### Через команду:
```bash
docker-compose down
```

**Что происходит:**
- Останавливаются все контейнеры
- Контейнеры остаются на диске (можно запустить снова)
- База данных сохраняется

---

## 🔄 Перезапуск проекта

### Windows:
```bash
restart.bat
```

### Linux/Mac:
```bash
chmod +x restart.sh
./restart.sh
```

### Через команды:
```bash
# Быстрый перезапуск (без остановки)
docker-compose restart

# Полный перезапуск (остановить и запустить заново)
docker-compose down
docker-compose up -d
```

**Когда использовать:**
- После изменения конфигурации
- После изменения файлов темы
- Если что-то не работает
- После обновления docker-compose.yml

---

## 🗑️ Полная очистка (удаление всех данных)

⚠️ **Внимание:** Это удалит базу данных и все данные WordPress!

### Windows:
```bash
docker-compose down -v
```

### Linux/Mac:
```bash
docker-compose down -v
```

**Что происходит:**
- Останавливаются контейнеры
- Удаляются volumes (база данных)
- Удаляются контейнеры
- Файлы WordPress в папке `./wordpress` остаются

**После очистки:**
```bash
docker-compose up -d
```
Затем нужно заново установить WordPress (выбрать язык, создать учетную запись).

---

## 📊 Полезные команды

### Посмотреть статус контейнеров:
```bash
docker-compose ps
```

### Посмотреть логи:
```bash
# Все логи
docker-compose logs

# Логи WordPress
docker-compose logs wordpress

# Логи базы данных
docker-compose logs db

# Логи в реальном времени
docker-compose logs -f

# Последние 50 строк логов WordPress
docker-compose logs wordpress --tail=50
```

### Войти в контейнер WordPress:
```bash
docker-compose exec wordpress bash
```

### Войти в контейнер MySQL:
```bash
docker-compose exec db mysql -u wordpress -pwordpress wordpress
```

### Перезапустить отдельный контейнер:
```bash
# Только WordPress
docker-compose restart wordpress

# Только база данных
docker-compose restart db
```

### Посмотреть использование ресурсов:
```bash
docker stats
```

### Остановить все контейнеры Docker:
```bash
docker stop $(docker ps -q)
```

### Удалить все контейнеры:
```bash
docker-compose down
docker rm -f $(docker ps -aq)
```

---

## 🔧 Решение проблем

### Порт 8080 занят:
```bash
# Остановить проект
docker-compose down

# Изменить порт в docker-compose.yml
# Например, с 8080 на 8082

# Запустить заново
docker-compose up -d
```

### Контейнеры не запускаются:
```bash
# Посмотреть логи
docker-compose logs

# Проверить статус
docker-compose ps

# Перезапустить
docker-compose restart
```

### Изменения в теме не применяются:
```bash
# Перезапустить WordPress контейнер
docker-compose restart wordpress

# Или полный перезапуск
docker-compose down
docker-compose up -d
```

### Проблемы с базой данных:
```bash
# Перезапустить базу данных
docker-compose restart db

# Подождать 30-60 секунд
# Проверить логи
docker-compose logs db
```

---

## 📋 Чек-лист после перезапуска

- [ ] Подождать 1-2 минуты после запуска
- [ ] Проверить статус: `docker-compose ps`
- [ ] Все контейнеры в статусе "Up"
- [ ] Открыть http://localhost:8080
- [ ] WordPress загружается
- [ ] Админ-панель доступна: http://localhost:8080/wp-admin

---

## 🎯 Типичные сценарии

### Сценарий 1: Обычный перезапуск
```bash
docker-compose restart
```

### Сценарий 2: После изменения файлов темы
```bash
docker-compose restart wordpress
```

### Сценарий 3: После изменения docker-compose.yml
```bash
docker-compose down
docker-compose up -d
```

### Сценарий 4: Полная очистка и переустановка
```bash
docker-compose down -v
docker-compose up -d
# Затем заново установить WordPress
```

### Сценарий 5: Остановка на ночь
```bash
docker-compose down
# Утром запустить: docker-compose up -d
```

---

## 💡 Советы

1. **Используйте скрипты** - они проще и быстрее
2. **Проверяйте логи** - если что-то не работает, смотрите логи
3. **Подождите после запуска** - контейнерам нужно время для инициализации
4. **Не удаляйте volumes без необходимости** - это удалит базу данных
5. **Сохраняйте изменения** - изменения в файлах темы сохраняются автоматически

---

## 📚 Дополнительная информация

- **Документация Docker**: https://docs.docker.com/
- **Документация Docker Compose**: https://docs.docker.com/compose/
- **Устранение неполадок**: см. `TROUBLESHOOTING.md`

