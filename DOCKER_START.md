# 🚀 Запуск проекта через Docker

## Да, вы можете запустить проект БЕЗ установки WordPress вручную!

Docker автоматически установит и настроит:
- ✅ WordPress
- ✅ MySQL базу данных
- ✅ phpMyAdmin
- ✅ Тему Simple UP Theme

## Быстрый старт (3 шага)

### Шаг 1: Убедитесь, что Docker запущен
Убедитесь, что Docker Desktop запущен на вашем компьютере.

### Шаг 2: Запустите проект

#### Вариант A: Использование скрипта (проще всего)

**Windows:**
```bash
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

#### Вариант B: Команда вручную

Откройте терминал в папке проекта и выполните:

```bash
docker-compose up -d
```

Эта команда:
- Скачает образы WordPress, MySQL и phpMyAdmin (если их нет)
- Создаст и запустит контейнеры
- Автоматически подключит тему Simple UP Theme
- Настроит базу данных

**Время ожидания:** 1-2 минуты при первом запуске

### Шаг 3: Откройте WordPress
Откройте браузер и перейдите:
- **WordPress**: http://localhost:8080
- **phpMyAdmin**: http://localhost:8081

## Установка WordPress

При первом запуске WordPress попросит вас завершить установку:

1. **Выберите язык**: Русский
2. **Заполните данные**:
   - Название сайта: `Simple UP` (или любое другое)
   - Имя пользователя: `admin` (или ваше)
   - Пароль: придумайте надежный пароль
   - Email: ваш email
3. **Нажмите "Установить WordPress"**

## Активация темы

После установки WordPress:

1. Войдите в админ-панель: http://localhost:8080/wp-admin
2. Перейдите в **Внешний вид → Темы**
3. Найдите тему **"Simple UP Theme"**
4. Нажмите **"Активировать"**

**Готово!** Тема уже установлена и доступна, так как она подключена через Docker volume.

## Использование шорткодов

Создайте новую страницу и добавьте шорткоды:

```
[simple_up_hero title="Добро пожаловать" description="Описание" button_text="Начать" button_link="#"]

[simple_up_features title="Наши возможности" columns="4"]

[simple_up_process title="Как мы работаем"]
```

## Управление контейнерами

### Через скрипты (рекомендуется):

**Windows:**
- Запустить: `start.bat`
- Остановить: `stop.bat`
- Перезапустить: `restart.bat`

**Linux/Mac:**
- Запустить: `./start.sh`
- Остановить: `./stop.sh`
- Перезапустить: `./restart.sh`

### Через команды:

#### Остановить проект:
```bash
docker-compose down
```

#### Перезапустить проект:
```bash
# Быстрый перезапуск
docker-compose restart

# Полный перезапуск (остановить и запустить заново)
docker-compose down
docker-compose up -d
```

#### Остановить и удалить данные:
```bash
docker-compose down -v
```
⚠️ **Внимание:** Это удалит базу данных!

#### Посмотреть логи:
```bash
docker-compose logs -f
```

#### Посмотреть статус:
```bash
docker-compose ps
```

📖 **Подробная инструкция:** см. `УПРАВЛЕНИЕ_ПРОЕКТОМ.md`

## Доступ к базе данных

### Через phpMyAdmin:
- URL: http://localhost:8081
- Сервер: `db`
- Пользователь: `wordpress`
- Пароль: `wordpress`

### Через командную строку:
```bash
docker-compose exec db mysql -u wordpress -pwordpress wordpress
```

## Структура данных

Все данные WordPress сохраняются в:
- **База данных**: Docker volume `db_data`
- **Файлы WordPress**: папка `./wordpress` (создается автоматически)
- **Тема**: автоматически подключена из `./wordpress-theme`

## Решение проблем

### Порт 8080 уже занят?
Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8082:80"  # Используйте другой порт
```

### Контейнеры не запускаются?
1. Проверьте, что Docker Desktop запущен
2. Проверьте логи: `docker-compose logs`
3. Убедитесь, что порты 8080 и 8081 свободны

### Тема не видна?
Тема автоматически подключена через volume. Если её нет:
1. Проверьте, что папка `wordpress-theme` существует
2. Перезапустите контейнеры: `docker-compose restart`

### Нужно сбросить WordPress?
```bash
docker-compose down -v
docker-compose up -d
```
Это удалит все данные и создаст чистую установку.

## Преимущества Docker

✅ **Не нужно устанавливать** WordPress, PHP, MySQL вручную
✅ **Автоматическая настройка** всех компонентов
✅ **Изоляция** - не влияет на другие проекты
✅ **Простое удаление** - просто удалите контейнеры
✅ **Портативность** - работает на любой системе с Docker
✅ **Тема автоматически подключена** - не нужно копировать файлы

## Что дальше?

1. Активируйте тему в WordPress
2. Создайте страницу с шорткодами
3. Настройте меню в **Внешний вид → Меню**
4. Начните редактировать контент!

## Полезные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Логи
docker-compose logs -f wordpress

# Войти в контейнер WordPress
docker-compose exec wordpress bash

# Войти в контейнер MySQL
docker-compose exec db mysql -u wordpress -pwordpress wordpress
```

---

**Готово!** Теперь у вас работает WordPress с темой Simple UP Theme без ручной установки! 🎉

