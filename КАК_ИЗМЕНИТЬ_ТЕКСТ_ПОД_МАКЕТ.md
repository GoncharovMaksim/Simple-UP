# Как изменить текст под макет Figma

## 📋 Структура блоков обновлена

Я обновил структуру блоков, чтобы они точно соответствовали макету Figma:

✅ **БЛОК 1**: Header + Hero Section (шапка и главный баннер)
✅ **БЛОК 6**: Features Section (две колонки с иконками)
✅ **БЛОК 7**: Two Column Section (текст слева, список справа)
✅ **БЛОК 8**: Process Section (4 элемента слева, 4 пронумерованных круга справа)

## 🔧 Как изменить текст под ваш макет

### Вариант 1: Изменить текст в шорткодах на странице

1. Откройте страницу в WordPress
2. Редактируйте шорткоды, изменяя параметры:

**БЛОК 1 (Hero):**
```
[simple_up_hero title="ВАШ ЗАГОЛОВОК" description="ВАШЕ ОПИСАНИЕ" button_text="ТЕКСТ КНОПКИ" button_link="#"]
```

**БЛОК 6 (Features):**
Сейчас отображается 2 колонки. Чтобы изменить текст, нужно отредактировать файл темы (см. ниже).

**БЛОК 7 (Two Column):**
```
[simple_up_two_column left_title="ЗАГОЛОВОК СЛЕВА" left_text="ТЕКСТ СЛЕВА" right_title="ЗАГОЛОВОК СПРАВА" right_text="ТЕКСТ СПРАВА" list_items="Пункт 1,Пункт 2,Пункт 3,Пункт 4"]
```

**БЛОК 8 (Process):**
Чтобы изменить текст, нужно отредактировать файл темы (см. ниже).

### Вариант 2: Изменить текст в файле темы (рекомендуется)

Отредактируйте файл: `wordpress-theme/inc/shortcodes.php`

#### Для БЛОК 6 (Features):

Найдите строки 73-86 и измените массив `$features`:

```php
$features = array(
    array(
        'icon' => '🚀',  // Иконка (можно изменить на другую)
        'title' => 'ВАШ ЗАГОЛОВОК',  // Заголовок из макета
        'description' => 'ВАШЕ ОПИСАНИЕ'  // Описание из макета
    ),
    array(
        'icon' => '💎',
        'title' => 'ВАШ ЗАГОЛОВОК',
        'description' => 'ВАШЕ ОПИСАНИЕ'
    )
);
```

#### Для БЛОК 8 (Process):

Найдите строки 123-151 и измените массивы `$process_items` и `$steps`:

```php
// Левая колонка
$process_items = array(
    array(
        'icon' => '📋',
        'title' => 'ВАШ ЗАГОЛОВОК',
        'description' => 'ВАШЕ ОПИСАНИЕ'
    ),
    // ... остальные элементы
);

// Правая колонка
$steps = array(
    array(
        'number' => '1',
        'title' => 'ВАШ ЗАГОЛОВОК',
        'description' => 'ВАШЕ ОПИСАНИЕ'
    ),
    // ... остальные шаги
);
```

## 📝 Шаги для точного соответствия макету

1. **Откройте макет Figma** и посмотрите точный текст в каждом блоке
2. **Откройте файл** `wordpress-theme/inc/shortcodes.php`
3. **Найдите нужный блок** (БЛОК 6 или БЛОК 8)
4. **Замените текст** на точный текст из макета
5. **Сохраните файл**
6. **Перезапустите WordPress** (или просто обновите страницу в браузере)

## 🎨 Изменение иконок

Вы можете заменить эмодзи-иконки на другие:

- 🚀 🎯 ⚡ 🔒 💡 💎 📋 🎨 ⚙️ ✅
- Или использовать любые другие эмодзи
- Или заменить на SVG иконки (требует изменения HTML)

## 🔄 После изменения файла темы

После изменения файла `shortcodes.php`:

1. **Сохраните файл**
2. **Обновите страницу** в браузере (Ctrl+F5)
3. **Проверьте результат**

Если изменения не применяются:
- Очистите кеш браузера
- Перезапустите WordPress контейнер: `docker-compose restart wordpress`

## 💡 Пример изменения текста

Если в макете написано:
- Заголовок: "Быстрая разработка"
- Описание: "Мы создаем решения быстро..."

То в файле измените на:

```php
array(
    'icon' => '🚀',
    'title' => 'Быстрая разработка',
    'description' => 'Мы создаем решения быстро...'
)
```

## 📋 Чек-лист

- [ ] Открыл макет Figma
- [ ] Посмотрел точный текст в каждом блоке
- [ ] Открыл файл `wordpress-theme/inc/shortcodes.php`
- [ ] Изменил текст в БЛОК 6 (если нужно)
- [ ] Изменил текст в БЛОК 8 (если нужно)
- [ ] Изменил текст в шорткодах на странице (БЛОК 1, БЛОК 7)
- [ ] Сохранил изменения
- [ ] Обновил страницу в браузере
- [ ] Проверил результат

## 🆘 Если нужна помощь

Если вы хотите, чтобы я изменил текст за вас, предоставьте точный текст из макета Figma для каждого блока, и я обновлю файлы.

---

**Важно:** Структура блоков теперь точно соответствует макету. Осталось только заменить текст на точный текст из вашего макета!

